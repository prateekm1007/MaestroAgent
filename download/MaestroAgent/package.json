{
  "name": "maestro-frontend",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "node build.cjs",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "dependencies": {
    "esbuild": "^0.28.1"
  }
}