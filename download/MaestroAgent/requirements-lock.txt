# MaestroAgent pinned dependencies for reproducible builds (FULL transitive lockfile).
#
# This file pins the EXACT versions tested in CI. Without this, a fresh
# `pip install` may pull incompatible pre-release versions (e.g., Starlette
# 1.3.1 drops WebSocket routes from the route table).
#
# Install: pip install -r requirements-lock.txt
#
# P23: this file is the reproducible-build evidence. Update it only after
# CI passes with the new versions.
#
# Generated: 2026-07-08 (after F4 fix verified at HEAD=286156a + Nerve integration).
# Verified by: scripts/run_full_regression.py (maestro_oem + maestro_api + maestro_auth suites).
#
# ── Tier 1: Direct application dependencies ─────────────────────────────────
# Web framework
fastapi==0.128.0
starlette==0.50.0
uvicorn==0.44.0
h11==0.16.0
click==8.1.8
python-multipart==0.0.24
itsdangerous==2.2.0
aiofiles==24.1.0
watchfiles==1.0.6
orjson==3.11.8
jiter==0.10.0

# Data validation
pydantic==2.12.5
pydantic-core==2.41.5
annotated-types==0.7.0
typing-extensions==4.14.0

# Async networking
httpx==0.28.1
httpcore==1.0.9
aiohttp==3.13.3
aiosignal==1.4.0
aiohappyeyeballs==2.6.1
async-timeout==5.0.1
attrs==26.1.0
multidict==6.7.1
yarl==1.23.0
propcache==0.4.1
frozenlist==1.6.0
websockets==16.0

# AnyIO (FastAPI/Starlette async abstraction)
anyio==4.13.0
sniffio==1.3.1

# HTTP/2 (httpx[http2])
h2==4.3.0
hpack==4.1.0
hyperframe==6.1.0

# Standard lib backports / helpers
idna==3.11
certifi==2026.2.25
charset-normalizer==3.4.7
urllib3==2.6.3
requests==2.32.5
six==1.17.0
python-dateutil==2.9.0.post0
packaging==26.0
tomli==2.2.1
exceptiongroup==1.3.1

# ── Tier 2: Auth + security ─────────────────────────────────────────────────
cryptography==44.0.3
cffi==2.0.0
pycparser==3.0
Authlib==1.6.9
PyJWT==2.12.1

# Email validation (pydantic[email])
email-validator==2.3.0
dnspython==2.8.0

# ── Tier 3: Database + persistence ──────────────────────────────────────────
# Note: SQLAlchemy + Postgres driver are wired in code but optional. SQLite is
# the default. These are pinned for the Postgres deployment path.
sqlalchemy==2.0.36
greenlet==3.4.0
# psycopg2-binary==2.9.10  # Uncomment for Postgres deployment
# asyncpg==0.30.0          # Uncomment for async Postgres deployment
redis==7.4.0

# ── Tier 4: Serialization + templating ──────────────────────────────────────
PyYAML==6.0.3
Jinja2==3.1.6
MarkupSafe==3.0.3

# Rich console + markdown rendering
rich==14.3.3
markdown-it-py==4.0.0
mdurl==0.1.2
Pygments==2.20.0

# Fuzzy string matching (terminology translation, semantic dedup)
RapidFuzz==3.14.3

# ── Tier 5: Testing (dev/CI only) ───────────────────────────────────────────
pytest==9.0.2
pytest-asyncio==1.3.0
pytest-cov==7.0.0
pluggy==1.6.0
iniconfig==2.3.0
Faker==40.1.2

# ── Prometheus metrics (optional, for observability) ────────────────────────
# prometheus-client==0.21.1  # Uncomment when metrics endpoint is enabled
