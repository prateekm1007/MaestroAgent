skills/
node_modules/

# Secrets — never commit
.env
.env.local
.env.*.local
*.pem
*.key
keys/
**/keys/*.json

# Build artifacts
dist/
build/
*.pyc
__pycache__/
.pytest_cache/
*.db

# Agentic-session debris (audit history, not production code)
# P-0 fix: these directories contain audit scripts + tool output from the
# engagement. They're useful locally but should not appear in a fresh clone
# or a Fortune 100 procurement review.
# NOTE: scripts/ is NOT ignored — download/MaestroAgent/scripts/pilot_smoke.sh
# must be tracked as a pilot deliverable.
tool-results/
upload/
maestro-audit/
